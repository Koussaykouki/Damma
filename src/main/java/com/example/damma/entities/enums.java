package com.example.damma.entities;

public class enums {
    public enum Color {
        BLACK, RED
    }

    public enum PieceType {
        REGULAR, KING
    }

    public enum GameStatus {
        ONGOING, BLACK_WINS, RED_WINS, DRAW
    }
}
